# MobilityDB-BerlinMOD documentation

This folder holds the **DocBook manual** for the BerlinMOD benchmark
(`benchmark.xml`, `datagen.xml`, `tutorial.xml`, `mobilitydb-berlinmod.xml`).

The **benchmark results and figures** are not here — they live in
[`../BerlinMOD/benchmarks/`](../BerlinMOD/benchmarks/):

- Cross-platform (MobilityDB / DuckDB / MobilitySpark) timings:
  [`CrossPlatform_timings.md`](../BerlinMOD/benchmarks/CrossPlatform_timings.md)
  and the `cross_platform_*.svg` figures.
- Streaming benchmark:
  [`streaming/`](../BerlinMOD/benchmarks/streaming/).
- The runnable query set and per-platform runners:
  [`bench/`](../BerlinMOD/benchmarks/bench/).
